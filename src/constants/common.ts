export const DEFAULT_PASSWORD = 'Admin@0123';

// The employee seeded by docker/installer/cli_install_config.yaml. The double space matches
// OrangeHRM's rendering of "firstName + middleName + lastName" when middleName is blank —
// the employee autocomplete option text won't match without it.
export const DEFAULT_EMPLOYEE_NAME = 'Qui  Ngo';
